<nav class="navbar has-background-light" role="navigation" aria-label="main navigation">
  <div class="navbar-brand ml-6">
    <a class="navbar-item" href=<%= root_path%>>
      <%= image_tag 'bookmark.png' %>
    </a>
      <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
      </a>
    </div>
    <div id="navbarBasicExample" class="navbar-menu">
      <div class="navbar-start">
        <%= link_to 'Home', root_path, class: 'navbar-item'%>
        <%= link_to 'Search', root_path, class: 'navbar-item'%>
        <% if user_signed_in? %>
          <%= link_to 'Reading Lists', root_path, class: 'navbar-item'%>
        <% end %>
      </div>
      <div class="navbar-end">
        <div class="navbar-item">
          <div class="buttons">
            <%if user_signed_in? %>
              <%= button_to 'Dashboard', user_path(current_user), method: :get, class: "button is-primary mr-2"%>
              <%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: "button is-primary"%>
            <%else%>
              <%= link_to 'Sign up', new_user_registration_path, class: "button is-primary"%>
              <%= link_to 'Log in', user_session_path, class:"button is-light" %>
            <%end%>
          </div>
        </div>
      </div>
    </div>
  </nav>
